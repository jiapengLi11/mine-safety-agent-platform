-r requirements.txt
transformers>=4.51,<5
huggingface-hub>=0.34,<1
safetensors>=0.4,<1
psutil>=6,<8

# PyTorch is intentionally omitted. Reuse the CUDA-enabled torch already
# installed in the laboratory ultralytics environment.
